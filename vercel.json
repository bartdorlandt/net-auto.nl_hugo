{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "git submodule update --init --recursive --depth 1 && hugo --gc --minify",
  "outputDirectory": "public",
  "framework": "hugo",
  "build": {
    "env": {
      "HUGO_VERSION": "0.165.0"
    }
  }
}